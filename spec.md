# 現場写真アップロードアプリ 仕様書（Draft v0.1）

最終更新: 2026-07-03

## 1. 概要

スマートフォン（Android / iPhone）で撮影した施工現場の写真を、Googleドライブへ簡単・確実にアップロードするための業務用アプリ。GitHub Pages上でPWA（Progressive Web App）として公開し、インストール不要でブラウザ／ホーム画面アイコンから利用できる。

## 2. 対象ユーザー・利用環境

- 社内の数名が利用する業務用アプリ（社内限定ではなく、URLを知っていれば誰でもアクセス可。ただしGoogleログインは必須）。
- Googleアカウントは全員共通の1つの共有アカウントを使用する想定。
- 対応端末：Android / iPhone（スマートフォンのブラウザ、PWAとしてホーム画面追加可能なもの）。
- 対応ブラウザ：Chrome（Android）、Safari（iPhone）を主対象とする。

## 3. 全体アーキテクチャ

- ホスティング：GitHub Pages（静的サイト、サーバーサイド処理なし）。
  - リポジトリ：https://github.com/TokujiTatewaki/site-photo-upload-project
  - 公開URL（予定）：https://tokujitatewaki.github.io/site-photo-upload-project/
- フロントエンド：PWA（HTML/CSS/JS、Service Workerによるオフラインキャッシュ・PWAインストール対応）。
- Google Drive連携：ブラウザ上のGoogle Identity Services（OAuth2）を用いたクライアントサイド認証。サーバーを持たないため、APIキー・シークレットは秘匿できず、ユーザーのGoogleログインで取得したアクセストークンを使ってDrive APIを直接呼び出す。
- OAuthスコープ：`drive.file`（アプリが作成・または明示的に開いたファイル／フォルダのみアクセス可能な限定スコープ）を使用する。
  - ルートフォルダは既存の共有フォルダを使用するため（下記）、初回のみGoogle Picker経由でこのフォルダを明示的に「開く」操作を行い、アプリにアクセス権を付与する。以降、アプリがこの配下に作成する顧客名／施工現場フォルダやファイルには自動的にアクセスできる。
- 保存先ルートフォルダ：既存の共有フォルダを使用する。
  - フォルダID：`1elNZfn2-OmjJ03Z4fgUPlBSNsBjnpugt`
  - URL：https://drive.google.com/drive/u/1/folders/1elNZfn2-OmjJ03Z4fgUPlBSNsBjnpugt
- データ保存先：
  - 写真本体：Googleドライブ（上記ルートフォルダ配下。フォルダ構成は4.6参照）
  - マスタデータ（顧客名／施工現場／施工年月の選択肢）：Googleドライブ上のJSONファイルで共有管理
  - アップロード履歴：端末ローカル（IndexedDB）＋ Googleドライブ上の共有履歴ファイル（全体分）の二重管理
  - アップロード再開用の一時情報（アップロードセッションURLなど）：端末ローカル（IndexedDB）

## 4. 機能要件

### 4.1 ログイン・認証

- アプリ起動時にGoogleログインを要求。共有のGoogleアカウントでログインする。
- ログイン状態は端末側で保持し、毎回のログイン操作を省略できるようにする（トークン有効期限切れ時は自動または半自動で再認証）。
- アプリ内でパスワードを保存・自動入力する機能は持たない（Googleの認証画面はGoogle側に委ねる）。
- 各端末での初回ログイン時、Google Pickerでルートフォルダ（3章参照）を1回だけ選択・許可させ、`drive.file`スコープでのアクセス権を取得する。以降はこの操作は不要。

### 4.2 マスタデータ管理（顧客名／施工現場／施工年月）

- 3つのプルダウン：①顧客名、②施工現場、③施工年月。
- ②施工現場は①顧客名に連動するカスケード式（顧客名を選ぶと、その顧客に紐づく施工現場のみ表示）。
- マスタデータはGoogleドライブ上のJSONファイル1つで管理し、全ユーザー・全端末で共有する。
- アプリ起動時／プルダウン操作時にマスタJSONを読み込み、選択肢を表示する。

### 4.3 新規項目追加

- 各プルダウンの選択肢に「その他（新規作成）」を用意する。
- 「その他（新規作成）」を選択すると、ポップアップ（モーダル）が表示され、テキスト入力で新しい値を登録できる。
- 新規追加した値は、その場でマスタJSON（Googleドライブ上）に反映し、以後のプルダウンにも表示されるようにする。
- ②施工現場を新規追加する場合は、選択中の①顧客名に紐づく形で登録する。

### 4.4 写真選択・圧縮

- ①②③の選択完了後、複数の写真ファイルを選択できる（端末のカメラロール／ファイル選択）。
- iPhoneのHEIC形式を含め、アップロード前にローカル側でJPEGに変換し、長辺2000px・JPEG品質80%程度にリサイズ／圧縮する。
- 圧縮処理は端末（ブラウザ）内で完結させ、圧縮後のファイルのみをアップロードする。

### 4.5 アップロード実行

- 「アップロード実行」ボタン押下で、選択した複数ファイルを一括アップロードする。
- Googleドライブ上のフォルダ（①顧客名＞②施工現場）が存在しない場合は、このアップロード実行のタイミングで初めて作成する（①②③の選択だけでは事前にフォルダを作らない）。
- 同名ファイルが既に存在する場合は、自動リネームして両方を保持する（上書きしない）。

### 4.6 フォルダ・ファイル命名規則

- Googleドライブのフォルダ構成：`①顧客名 ＞ ②施工現場` の2階層。
- ③施工年月はフォルダを分けず、ファイル名のプレフィックスとして付与する。
- ファイル名規則：`YYYYMM_[顧客名]_[施工現場]_xxx.jpg`
  - `xxx` は同一フォルダ内での連番（000〜999、3桁ゼロ埋め）。
  - 同名衝突時は連番を繰り上げて自動採番する。

### 4.7 レジューム（再開）機能

- 通信環境の悪化・切断でアップロードが中断した場合、再接続時に中断箇所から再開できるようにする（Google Drive Resumable Upload APIのアップロードセッションURLを利用）。
- アップロードセッション情報（セッションURL、アップロード済みバイト数、対象ファイル情報等）は端末ローカル（IndexedDB）に保持し、アプリ再起動後も再開可能とする。
- 再開できない場合（セッション期限切れ等）は、そのファイルのみ最初からアップロードし直す。

### 4.8 アップロード履歴

- 各ファイルについて、アップロードの完了／未完了（進行中・失敗・中断）の状態を確認できる画面を用意する。
- 履歴は以下の2箇所で管理する。
  - 端末ローカル履歴（IndexedDB）：その端末で行った操作の履歴。
  - 全体共有履歴（Googleドライブ上の履歴ファイル）：全端末・全ユーザー分の履歴を集約し、誰がいつ何をアップロードしたか横断的に確認できるようにする。
- 失敗・未完了のファイルは再アップロードできるようにする。

## 5. 非機能要件

- PWA対応：Web App Manifest、Service Workerを実装し、ホーム画面への追加、オフライン時の画面表示に対応する。
- オフライン時の挙動：写真選択・圧縮まではオフラインでも可能とし、アップロードはネットワーク復帰時に自動的に再開・実行する（4.7のレジューム機能と連携）。
- パフォーマンス：圧縮処理・アップロードとも、スマートフォン単体で実用的な速度で完了すること。
- セキュリティ：Googleアカウントの認証情報（パスワード等）をアプリ内に平文保存しない。
- iOS Safari（PWA）での中断対応：画面ロック／アプリ切り替えによりアップロードが中断される可能性があるため、（1）4.7のレジューム機能で再開を担保し、（2）アップロード中は「画面を閉じないでください」という注意書きを画面上に表示する、の2点で対応する（Wake Lock APIによるスリープ防止などの追加対策は今回は行わない）。
- 対応ファイル種別：写真（画像）のみを対象とし、動画ファイルは対象外とする。

## 6. 画面構成（想定）

1. ログイン画面（Googleログイン）
2. メイン画面
   - ①顧客名／②施工現場／③施工年月のプルダウン（各「その他（新規作成）」あり）
   - 新規作成用ポップアップ（テキスト入力）
   - 写真選択ボタン、選択済みサムネイル一覧
   - 「アップロード実行」ボタン
   - アップロード進捗表示（全体／ファイルごと）
3. アップロード履歴画面
   - 端末ローカル履歴一覧
   - 全体共有履歴一覧（完了／未完了�